#!/bin/bash

####configPassword

echo "Put the name/s of the user/s to auto-login preceding a blank space. user1 user2..."
echo ""
echo "If you put only blank space, always will ask for pass for all users"
read USER

FILE=/etc/lightdm/lightdm.conf

sudo sed "/autologin-user=/ s/$/$USER/" $FILE